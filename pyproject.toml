[project]
name = "closo"
version = "0.1.0"
description = "Self-verifying three-way reconciliation agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q --strict-markers"
